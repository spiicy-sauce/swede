; Language injections for Swede.
;
; Swede embeds no sub-languages at present (free-prose strings and metadata
; values are opaque). This file is intentionally minimal and exists so consumers
; can rely on a stable query path.
